server:
  port: ":8081"

services:
  authn_url: "http://localhost:8082"
  authz_url: "http://localhost:8083"
  estate_url: "http://localhost:8084"

auth:
  cache_ttl: "5m"
  session_secret: "change-this-in-production"

log:
  level: "info"
